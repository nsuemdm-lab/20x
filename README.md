# 🎓 Универсальный LMS-каркас (Flask)

Этот проект — готовая основа для вашей курсовой работы. Он автоматически настраивается под любое имя пользователя и субдомен на PythonAnywhere.

## ⚙️ Установка на PythonAnywhere (Ваш субдомен)

1. **Создание проекта:**
   - Зайдите в консоль Bash и склонируйте репозиторий:
     ```bash
     git clone https://github.com/nsuemdm-lab/20x.git mysite
     ```
   - Установите зависимости:
     ```bash
     pip3 install --user flask-sqlalchemy
     ```

2. **Настройка Web App:**
   - В панели PythonAnywhere перейдите во вкладку **Web**.
   - Нажмите **Add a new web app**.
   - Выберите **Manual Configuration** -> **Python 3.10** (или новее).
   - В поле **Source code** укажите: `/home/ВАШ_ЛОГИН/mysite`

3. **Настройка WSGI (Важно):**
   - Кликните на ссылку **WSGI configuration file**.
   - Удалите всё содержимое и вставьте этот универсальный код:
     ```python
     import sys
     import os

     path = '/home/' + os.environ.get('USER') + '/mysite'
     if path not in sys.path:
         sys.path.append(path)

     from flask_app import app as application
     ```

4. **Запуск:**
   - Вернитесь на вкладку **Web** и нажмите **Reload**.
   - Ваш сайт будет доступен по адресу: `https://ВАШ_ЛОГИН.pythonanywhere.com`

## 🧩 Особенности архитектуры
- **Пути:** Использование `os.path.dirname(__file__)` позволяет приложению находить базу данных `lms.db` независимо от того, какой у вас логин.
- **База:** Таблицы и тестовые данные создаются автоматически при первом открытии сайта.
- **Безопасность:** Доступ к урокам проверяется на стороне сервера через таблицу `Enrollment`.

## 🛠 Задания для доработки
- Реализуйте страницу регистрации (`/register`).
- Добавьте возможность загрузки аватара студента.
- Сделайте админ-панель для создания новых уроков.
