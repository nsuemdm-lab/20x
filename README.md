# Методические указания: Каркас LMS-системы на Flask

Данный репозиторий содержит базовый шаблон (каркас) системы управления обучением (Learning Management System). Проект предназначен для изучения основ веб-разработки на Python, работы с реляционными базами данных и реализации логики доступа к контенту.

Студенты могут использовать этот код как фундамент для своих курсовых работ, расширяя функционал и изменяя бизнес-логику.

## 📌 Основной функционал
1.  **Каталог курсов**: Вывод списка доступных программ.
2.  **Модель доступа**: Ограничение просмотра уроков (только после «покупки»).
3.  **Прогресс обучения**: Динамический расчет процента прохождения курса на основе завершенных уроков.
4.  **Автоматическая инициализация**: Создание базы данных и наполнение её тестовыми данными при первом запуске.

---

## 🛠 Технологический стек
*   **Язык**: Python 3.x
*   **Фреймворк**: Flask
*   **ORM**: SQLAlchemy (для работы с БД через объекты Python)
*   **БД**: SQLite (легкая база данных в виде одного файла)
*   **Frontend**: HTML5 + Bootstrap 5

---

## 📂 Структура проекта
```text
├── flask_app.py         # Основной файл приложения (логика, модели БД, роуты)
├── lms.db               # Файл базы данных (создается автоматически)
└── templates/           # Папка с HTML-шаблонами
    ├── index.html       # Главная страница (список курсов)
    ├── course.html      # Страница курса и прогресс-бар
    └── lesson.html      # Контент урока
```

---

## 🚀 Инструкция по локальной установке

1. **Клонируйте репозиторий или скачайте файлы:**
   ```bash
   git clone https://github.com/dmbingo/spo.git
   cd lms-framework
   ```

2. **Создайте виртуальное окружение:**
   ```bash
   python -m venv venv
   # Для Windows:
   venv\Scripts\activate
   # Для macOS/Linux:
   source venv/bin/activate
   ```

3. **Установите необходимые библиотеки:**
   ```bash
   pip install flask flask-sqlalchemy
   ```

4. **Запустите приложение:**
   ```bash
   python flask_app.py
   ```
   После запуска проект будет доступен по адресу: `http://127.0.0.1:5000`

---

## 🌍 Деплой на PythonAnywhere.com

При переносе проекта на хостинг PythonAnywhere учтите следующие моменты:
1.  **Пути к БД**: В коде используется `os.path.abspath`, чтобы сервер всегда находил файл `lms.db` вне зависимости от рабочей директории.
2.  **WSGI-файл**: В настройках веб-панели (Web tab) убедитесь, что путь к приложению указан верно, а импорт выглядит как `from flask_app import app as application`.
3.  **Static**: Если вы добавите свои CSS/JS файлы, не забудьте настроить пути к Static файлам в панели управления хостингом.

---

## 🧠 Логика работы (для курсовой работы)

### 1. Схема базы данных (ER-диаграмма)
*   **User**: Хранит данные студентов.
*   **Course**: Заголовок и описание курса.
*   **Lesson**: Привязан к курсу (связь One-to-Many).
*   **Enrollment**: Таблица-связка. Если в ней есть запись (User + Course), значит курс «куплен».
*   **Progress**: Таблица-связка. Если в ней есть запись (User + Lesson), значит урок пройден.

### 2. Алгоритм расчета прогресса
Прогресс рассчитывается в функции `course_detail` по формуле:
$$Progress = \left( \frac{\text{Кол-во пройденных уроков}}{\text{Общее кол-во уроков в курсе}} \right) \times 100$$

### 3. Защита контента
Доступ к маршруту `/lesson/<id>` проверяется через запрос к таблице `Enrollment`. Если запись отсутствует, сервер возвращает ошибку `403 Forbidden`.

---

## 📝 Задания для самостоятельного расширения (Идеи для КР)
Чтобы превратить этот каркас в полноценную курсовую работу, рекомендуется реализовать:
1.  **Система регистрации и входа**: Добавить Flask-Login и хэширование паролей (Werkzeug).
2.  **Личный кабинет преподавателя**: Возможность добавлять/редактировать курсы и уроки через веб-интерфейс.
3.  **Домашние задания**: Создать модель `Homework` и возможность загрузки файлов (отправка студентом — проверка учителем).
4.  **Тестирование**: Добавить тесты в конце каждого урока с автоматической проверкой.
5.  **Интеграция оплаты**: Симуляция платежной системы или интеграция реального API (например, ЮKassa).

---
*Данный каркас подготовлен в учебных целях. При использовании в реальных проектах не забудьте сменить `SECRET_KEY` и настроить безопасность базы данных.*
